import React from 'react';
import s from './HomeView.module.css';

const HomeView = () => {
  <div className={s.container}>
    <h1 className={s.title}>
      Hello! This is a service for storing your contacts, go sign up or log in
      ...
    </h1>
  </div>;
};

export default HomeView;
